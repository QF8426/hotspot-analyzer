server:
  port: 8080

spring:
  datasource:
    url: jdbc:mysql://localhost:3306/hotspot_analyzer?useUnicode=true&characterEncoding=utf-8&serverTimezone=Asia/Shanghai
    username: root
    password: 936624
    driver-class-name: com.mysql.cj.jdbc.Driver

mybatis:
  mapper-locations: classpath:mapper/*.xml
  type-aliases-package: com.example.hotspotanalyzer.entity
  configuration:
    map-underscore-to-camel-case: true

logging:
  level:
    com.example.hotspotanalyzer.mapper: debug